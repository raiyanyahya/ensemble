# Copy to evals/.env (git-ignored) and fill in FRESH, valid keys, then ask me to
# run the harder eval. All three are needed for the full design:
#   - OPENAI_API_KEY    -> gpt-4o-mini (cheap single + ensemble member)
#   - ANTHROPIC_API_KEY -> Claude Haiku 4.5 (cheap single + ensemble) AND
#                          Claude Sonnet 4.6 (the strong baseline)
#   - DEEPSEEK_API_KEY   -> DeepSeek Chat (cheap single + ensemble member)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
